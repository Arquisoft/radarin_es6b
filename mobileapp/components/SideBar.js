import React from 'react';
import {View,Text,StyleSheet,ScrollView,ImageBackgorund,Image, SliderBase} from 'react-native';


export default SideBar = props => (
    <ScrollView>
        <Text>SideBar</Text>
    </ScrollView>
);

const styles = StyleSheet.create({

});